import 'package:flutter/material.dart';
import 'package:obs_animated_banners/src/core/theme/colors.dart';

final BorderRadius borderBig = BorderRadius.circular(12);

const BorderSide borderSideNormal =
    BorderSide(color: UiColors.primaryColor, width: 0.5);

final ShapeBorder shapeBig = RoundedRectangleBorder(
  borderRadius: borderBig,
);
